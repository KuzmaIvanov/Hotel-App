<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.HotelApp" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="fontFamily">@font/sf_pro_display_regular</item>
        <item name="materialButtonStyle">@style/Widget.App.Button</item>
        <item name="colorPrimary">@color/blue_main</item>
    </style>

    <style name="Widget.App.Button" parent="Widget.Material3.Button">
        <item name="materialThemeOverlay">@style/ThemeOverlay.App.Button</item>
        <item name="shapeAppearance">@style/ShapeAppearance.App.SmallComponent</item>
    </style>

    <style name="ShapeAppearance.App.SmallComponent" parent="ShapeAppearance.Material3.SmallComponent">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">15dp</item>
    </style>

    <style name="ThemeOverlay.App.Button" parent="ThemeOverlay.Material3.Button.TextButton">
        <item name="colorContainer">@color/blue_main</item>
        <item name="colorOnContainer">@color/white</item>
    </style>

    <style name="TextInputLayout" parent="Widget.Material3.TextInputLayout.FilledBox">
        <item name="boxCornerRadiusTopStart">10dp</item>
        <item name="boxCornerRadiusTopEnd">10dp</item>
        <item name="boxCornerRadiusBottomStart">10dp</item>
        <item name="boxCornerRadiusBottomEnd">10dp</item>
        <item name="errorEnabled">true</item>
        <item name="boxStrokeWidth">0dp</item>
        <item name="boxStrokeWidthFocused">0dp</item>
        <item name="boxStrokeColor">@color/gray_edit_text</item>
        <item name="boxBackgroundColor">@color/gray_edit_text</item>
        <item name="hintTextColor">@color/gray_hint_edit_text</item>
    </style>

    <style name="Theme.HotelApp" parent="Base.Theme.HotelApp" />
</resources>